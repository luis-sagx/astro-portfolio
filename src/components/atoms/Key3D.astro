---
import TechIcon from './TechIcon.astro';
import { resolveTechBadgeColor, shouldUseWhiteBadgeIcon } from '../../utils/tech-icons';

interface Props {
  name: string;
  isWide?: boolean;
  widthUnits?: number;
  mini?: boolean;
}

const { name, isWide = false, widthUnits = 1, mini = false } = Astro.props;

const colors = resolveTechBadgeColor(name);
const useWhiteIcon = shouldUseWhiteBadgeIcon(name);

const bgColor = colors.bg;
---

<div
  class={`key-3d ${isWide ? 'key-wide' : ''} ${mini ? 'key-mini' : ''}`}
  style={`--bg-color: ${bgColor}; --key-units: ${isWide ? 2 : widthUnits};`}
>
  <div class="key-back"></div>
  <div class="key-top">
    <span class="key-icon">
      <TechIcon name={name} size={mini ? 18 : 22} className="shrink-0" />
    </span>
  </div>
</div>

<style>
  .key-3d {
    --key-radius: 8px;
    --key-units: 1;
    --key-unit-size: 56px;
    --key-gap: 8px;
    --bg-color: #3b82f6;
    
    position: relative;
    display: inline-block;
    cursor: pointer;
    pointer-events: auto;
    z-index: 10;
    transform-style: preserve-3d;
  }

  .key-back {
    position: absolute;
    top: 0;
    left: 0;
    width: calc((var(--key-unit-size) * var(--key-units)) + (var(--key-gap) * (var(--key-units) - 1)));
    height: 56px;
    background: color-mix(in srgb, var(--bg-color) 55%, black);
    border-radius: var(--key-radius);
    transform: translate(4px, 7px);
    z-index: 0;
  }

  .key-top {
    position: relative;
    z-index: 1;
    padding: 12px;
    width: calc((var(--key-unit-size) * var(--key-units)) + (var(--key-gap) * (var(--key-units) - 1)));
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--bg-color);
    border-radius: var(--key-radius);
    transition: transform 0.15s ease-out;
  }

  .key-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    filter: drop-shadow(0 1px 2px rgba(0, 0, 0, 0.2));
  }

  .key-3d:hover .key-top {
    transform: translate(2px, 5px);
  }

  :global(.dark) .key-top {
    /* Border and box-shadow removed */
  }

  @media (max-width: 640px) {
    .key-3d {
      --key-unit-size: 44px;
      --key-gap: 5px;
    }

    .key-top {
      height: 44px;
      padding: 10px;
    }

    .key-back {
      height: 44px;
      transform: translate(3px, 3px);
    }

    .key-icon {
      transform: scale(0.9);
    }
  }

  /* Mini variant for cards */
  .key-mini {
    --key-unit-size: 38px;
    --key-gap: 3px;
  }

  .key-mini .key-top {
    height: 38px;
    padding: 6px;
  }

  .key-mini .key-back {
    height: 38px;
    transform: translate(2px, 3px);
  }

  .key-mini .key-icon {
    transform: scale(0.75);
  }
</style>
